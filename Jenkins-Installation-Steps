--------------------------------------------------------------------------------------------------------------------------------------
Step 1: Update the system

sudo apt update -y
sudo apt upgrade -y
sudo apt install -y curl gnupg2 software-properties-common

--------------------------------------------------------------------------------------------------------------------------------------
Step 2: Install Java 17

sudo apt install -y openjdk-17-jdk
java --version
# Expected output: openjdk 17.x.x ...

--------------------------------------------------------------------------------------------------------------------------------------
Step 3: Set JAVA_HOME permanently

echo "export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" | sudo tee /etc/profile.d/jdk17.sh
echo "export PATH=$JAVA_HOME/bin:$PATH" | sudo tee -a /etc/profile.d/jdk17.sh
source /etc/profile.d/jdk17.sh
echo $JAVA_HOME
java --version

--------------------------------------------------------------------------------------------------------------------------------------
Step 4: Add Jenkins repository and key

curl -fsSL https://pkg.jenkins.io/debian/jenkins.io.key | sudo tee /usr/share/keyrings/jenkins-keyring.asc > /dev/null
echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian binary/" | sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update

--------------------------------------------------------------------------------------------------------------------------------------
Step 5: Install Jenkins

sudo apt install -y jenkins

--------------------------------------------------------------------------------------------------------------------------------------
Step 6: Configure Jenkins to use Java 17

sudo nano /etc/default/jenkins

# Add or update these lines:
JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
HTTP_PORT=8080

# Save and exit (Ctrl+X, Y, Enter)

--------------------------------------------------------------------------------------------------------------------------------------
Step 7: Start and enable Jenkins

sudo systemctl daemon-reexec
sudo systemctl start jenkins
sudo systemctl enable jenkins
sudo systemctl status jenkins
# Expected output: Active: active (running)

--------------------------------------------------------------------------------------------------------------------------------------
Step 8: Verify Jenkins

curl http://localhost:8080
# Or open in browser: http://<server-ip>:8080

--------------------------------------------------------------------------------------------------------------------------------------
Step 9: Open firewall port (if using UFW)

sudo ufw allow 8080
sudo ufw reload
sudo ufw status

--------------------------------------------------------------------------------------------------------------------------------------

Note: This setup ensures Jenkins runs with Java 17, which is the minimum required version.
